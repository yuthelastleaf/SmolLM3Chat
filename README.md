# SmolLM3Chat
SmolLM3模型的测试，验证小模型部署的可行性以及实用程度，但是中文还是不方便自己去处理了

# chatglm
chatglm3 模型小一些，但是中文效果还不错，但是一个不好的地方是，chatglm3不是标准的transformers模型，所以需要用到chatglm3的专用库，微调起来还得专门用chatglm3的微调代码，不太方便。

# qwen2.5
qwen2.5模型效果还不错，中文也可以接受，模型大小适中,标准的transformers模型，方便微调和部署。最后还是基于这个来吧。
